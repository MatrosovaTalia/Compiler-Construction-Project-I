type c is integer;
type b is boolean;

routine my_routine(v: integer, y: integer) : integer is
    var ab : integer;
    var c is 3;

    while true loop
        var c is 3;
        var b is false;
    end
    if true then
        return 1;
    else
        return 2;
    end
end
